# Aura Kalshi MCP Server

Functional read-only API client with strict governance boundaries.

## Governance Lock
Financial read actions (e.g. `getPortfolio`) are explicitly blocked from headless execution.
