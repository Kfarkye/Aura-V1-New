export const asyncHandler = (fn: any) => (req: any, res: any, next: any) => { Promise.resolve(fn(req, res, next)).catch(next); }; export const securePathResolve = () => ''; export const safeParseJson = <T = any>(data: any): T | null => { try { return JSON.parse(data); } catch { return null; } }; export const resolvePrincipal = async () => ({ principal_id: 'anonymous', roles: ['execute:ai_chat'] }); export type GovernancePrincipal = any;